// Shared unit list for every unit dropdown in the app (product pricing
// tiers, the invoice builder's custom-item form) — one list so they can
// never drift into offering different choices for the same concept.
export const UNITS = ['box', 'Piece', 'kg', 'bag', 'block', 'slab'];
